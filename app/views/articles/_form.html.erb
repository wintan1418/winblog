<%= form_with(model: article, data: { controller: "editor" }) do |form| %>
  <% if article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>
  <%= form.hidden_field :content, id: 'article_content_hidden' %>


  <div id="editorjs" data-controller="editor">
    <div data-target="editor.article_content" class="editor-content"></div>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
